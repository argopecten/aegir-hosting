hosting_package.settings:
  type: config_object
  label: 'Hosting package settings'
  mapping:
    hosting_blocked_profiles:
      type: sequence
      label: 'Blocked profiles'
      sequence:
        type: string
