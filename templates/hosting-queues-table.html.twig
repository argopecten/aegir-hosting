{#
/**
 * @file
 * Template for hosting queues table.
 *
 * Available variables:
 * - table: The queues table render array.
 * - queues: Array of queue data.
 * - attributes: HTML attributes for the container element.
 */
#}
<div{{ attributes }}>
  {{ table }}
</div>
